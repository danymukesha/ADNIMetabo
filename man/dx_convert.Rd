% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dx_convert.R
\name{dx_convert}
\alias{dx_convert}
\title{Generate diagnostic conversion indicators for ADNI Data}
\usage{
dx_convert(data, dx_only = TRUE, check_validity = TRUE)
}
\arguments{
\item{dx_only}{is logical indicating whether to return
only diagnosis-related columns (default: TRUE)}

\item{check_validity}{is a logical indicating whether to validate
diagnostic codes (default: TRUE)}

\item{dt}{is a data frame containing ADNI clinical data}
}
\value{
The function should return a tibble with original columns
(if dx_only = FALSE) and added conversion indicators:
\itemize{
\item{\code{ad_conv}: conversion from any non-AD baseline
to dementia (1 = conversion)}
\item{\code{cn_to_ad}: conversion from cognitively normal (CN) to dementia}
\item{\code{cn_to_mci}: conversion from CN to mild cognitive impairment (MCI)}
\item{\code{emci_to_ad}: conversion from early MCI to dementia}
\item{\code{lmci_to_ad}: conversion from late MCI to dementia}
}
}
\description{
With this function you will be able to create binary flags
tracking transitions between baseline and follow-up diagnoses
in the ADNI dataset. Optimized for performance and compatibility
with tidyverse workflows.
}
\section{Input requirements}{

\itemize{
\item should contain columns: RID (participant ID), VISCODE (visit code)
\item should contain diagnostic columns: DX_bl (baseline diagnosis),
DX (current diagnosis)
\item expected diagnostic values: "CN", "MCI", "EMCI", "LMCI", "Dementia"
}
}

\examples{
\dontrun{
library(dplyr)

# eg.
adni_data <- tibble(
  RID = c(1, 2, 3),
  VISCODE = c("bl", "m12", "m24"),
  DX_bl = c("CN", "EMCI", "LMCI"),
  DX = c("MCI", "Dementia", "Dementia")
)

# generate conversion indicators
dx_convert(adni_data, dx_only = TRUE)
}

}
